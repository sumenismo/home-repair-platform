# Test database — safe to commit, contains no secrets.
#
# One-time local setup (run once after cloning):
#   createdb -U postgres home_repair_test
#   pnpm db:migrate:test
DATABASE_URL=postgres://localhost/home_repair_test
